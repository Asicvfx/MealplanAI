# 🚀 Быстрый старт

## За 5 минут до первого плана питания!

### Шаг 1: Установка зависимостей (2 минуты)

```bash
# Активируйте виртуальное окружение
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows

# Установите пакеты
pip install -r requirements.txt
```

### Шаг 2: Настройка API ключа (2 минуты)

1. Получите API ключ на [OpenAI Platform](https://platform.openai.com/api-keys)
2. Создайте файл `.env` в корне проекта:

```bash
echo "OPENAI_API_KEY=ваш_ключ_здесь" > .env
```

Подробнее: [ENV_SETUP.md](ENV_SETUP.md)

### Шаг 3: Запуск (1 минута)

```bash
# Интерактивный режим
python main.py

# Или запустите пример
python test_example.py
```

## 📝 Пример использования

```python
from src.models.schemas import UserInput
from src.graph.workflow import MealPlannerWorkflow

# Ваши данные
user_input = UserInput(
    goal="lose_weight",      # lose_weight, gain_weight, maintain
    gender="male",           # male, female
    weight=85.0,            # кг
    height=180.0,           # см
    age=30,                 # лет
    preferences="веган"     # любые предпочтения
)

# Создание плана
workflow = MealPlannerWorkflow()
result = workflow.run(user_input)

# План готов!
print(result.final_plan)
```

## 📤 Результаты

После выполнения получите:
- `meal_plan.json` - План в JSON
- `meal_plan.md` - Читаемый Markdown
- Консольный вывод с детальной информацией
- Список покупок на неделю

## 🎯 Что дальше?

1. Попробуйте разные параметры
2. Изучите [README.md](README.md) для деталей
3. Посмотрите код агентов в `src/agents/`
4. Настройте параметры в `src/utils/config.py`

## ⚡ Быстрые команды

```bash
# Интерактивный режим
python main.py

# Запуск тестов
python test_example.py

# Проверка настроек
python -c "from src.utils.config import OPENAI_API_KEY; print('✅ OK' if OPENAI_API_KEY else '❌ Error')"
```

## 🆘 Проблемы?

**Ошибка: "OPENAI_API_KEY не найден"**
→ Проверьте файл `.env`, должна быть строка: `OPENAI_API_KEY=sk-...`

**Ошибка: "No module named 'langchain'"**
→ Установите зависимости: `pip install -r requirements.txt`

**Ошибка: "Invalid API key"**
→ Проверьте ключ на [OpenAI Platform](https://platform.openai.com/api-keys)

## 💡 Советы

- Используйте конкретные предпочтения: "веган", "без глютена", "кето"
- Система поддерживает любые диеты - просто опишите
- Результаты сохраняются автоматически
- Можно запускать несколько раз с разными параметрами

---

Готово! Теперь у вас есть персональный AI-диетолог! 🎉


# Настройка переменных окружения

## Получение API ключа OpenAI

1. Перейдите на сайт [OpenAI](https://platform.openai.com/)
2. Войдите в свой аккаунт или зарегистрируйтесь
3. Перейдите в раздел [API Keys](https://platform.openai.com/api-keys)
4. Создайте новый секретный ключ
5. Скопируйте ключ (вы увидите его только один раз!)

## Создание файла .env

Создайте файл `.env` в корне проекта:

```bash
# На macOS/Linux:
touch .env

# На Windows:
type nul > .env
```

## Добавление ключа в .env

Откройте файл `.env` в текстовом редакторе и добавьте:

```
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

Замените `sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx` на ваш реальный API ключ.

## Проверка

После настройки файла `.env`, запустите тест:

```bash
python -c "from src.utils.config import OPENAI_API_KEY; print('✅ API Key загружен!' if OPENAI_API_KEY else '❌ Ошибка: API Key не найден')"
```

Если все настроено правильно, вы увидите: "✅ API Key загружен!"

## Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитьте файл `.env` в git
- Файл `.env` уже добавлен в `.gitignore`
- Не делитесь вашим API ключом с другими
- Если ключ был скомпрометирован, удалите его и создайте новый на платформе OpenAI

## Стоимость использования

💰 **Тарификация OpenAI:**
- GPT-4 Turbo: ~$0.01 за 1K токенов (input) и ~$0.03 за 1K токенов (output)
- Для тестирования одного плана питания: примерно $0.10-0.30
- Новым пользователям обычно дают бесплатные кредиты для начала работы

Проверить баланс и использование: [OpenAI Usage](https://platform.openai.com/usage)
